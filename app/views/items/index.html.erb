<div class="my-4">
	<%= link_to "Items", :items %> / 
	<% unless params[:label].blank? %>
		labeled: <%= params[:label] %>
		-
		<%= link_to "Clear", :items %>
	<% end %>
</div>

<%== pagy_nav(@pagy) %>

<div class="my-4">
  <%= render @items %>
</div>

<%== pagy_nav(@pagy) %>


<div class="my-4">
  <%= render 'form', item: Item.new %>
</div>
	 
