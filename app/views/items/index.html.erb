<div class="my-4">
  <%= render "items/breadcrumbs", item: nil %>
</div>

<%== pagy_nav(@pagy) %>

<div class="my-4">
	<%= turbo_frame_tag "items" do %>
		<%= render @items %>
	<% end%>
</div>

<%== pagy_nav(@pagy) %>


<div class="my-4">
	<%= turbo_frame_tag Item.new do %>
		<%= render 'form', item: Item.new %>
	<% end%>
</div>
	 
