<!DOCTYPE html>
<html>
  <head>
    <title>CocktailOnRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header class="mb-4">
      <div class="container mx-auto p-4 border-b flex flex-col leading-8 md:flex-row md:gap-x-2">
			 
        <%= link_to "Cocktails", :root, class: "mr-4 font-bold"%>

				<% if user_signed_in? %>
					<%= link_to "Recipes", :recipes, class: "" %>
					<%= link_to "+ Recipe", :new_recipe, class: "" %>
					<%= link_to "Available", :available_recipes, class: "" %>

					<%= link_to "Items", :items, class: "" %>
					<%= link_to "+ Item", :new_item, class: "" %>
					<%= link_to "My Bar", :my_bar_items, class: "" %>
					<%= link_to "Wish List", :my_wish_items, class: "" %>

					<% if user_signed_in? %>
						<%= link_to current_user, :edit_user_registration, class: "md:ml-auto" %>
						<%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :delete} %>
					<% end %>
				<% end %>
      </div>
    </header>

    <div class="flash">
      <%= apply_notice %>
    </div>

    <main>
      <div class="container mx-auto px-4">
        <%= yield %>
      </div>
    </main>

    <footer class="my-48">
      <div class="container mx-auto px-4">
        &copy; Daniel Cheang 2022-2025
      </div>
    </footer>
  </body>
</html>
