<div class="flex items-center my-1">
	<span class="mr-2">
		<%= ingredient.quantity %>
		<%= ingredient.unit %>
	</span>
	<span class="mr-2">
		<%= link_to ingredient.name, items_path(q: ingredient.name) %>

		<% if current_user.has_ingredient? ingredient.name %>
			<i class="fa-solid fa-check"></i>
		<% end %>
	</span>
</div>